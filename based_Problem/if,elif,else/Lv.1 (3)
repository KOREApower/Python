# problem description
수학 , 과학 성적을 입력받고 평균이 90점 이상이면 A 평균이 80점 이상이면 평균이 70점이상이면 C 나머지는 D가 나오도록 프로그램하세요.

# solve .py
math = float (input("수학 점수:"))
sci = float (input("과학 점수:"))

avg = (math + sci) / 2 

if avg >= 90 :
    print ("A")

elif avg >= 80:
    print ("B")

elif avg >= 70 :
    print ("C")

else :
    print ("D")
